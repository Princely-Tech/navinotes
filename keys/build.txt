ios build:
flutter build ios --release


Archive for App Store (IMPORTANT)
open ios/Runner.xcworkspace
In Xcode:
	1.	Select Any iOS Device (arm64)
	2.	Menu → Product → Archive

Wait 2–10 minutes.

When archive completes, Organizer will open.